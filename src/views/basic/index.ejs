<!DOCTYPE html>
<html>
<head>
<title>DASHBOARD LOGIN</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<style>
              .container {
            display: flex;
            width: 100%;
            padding: 40px 0;
            align-items: center;
            justify-content: center;
          }
          
          .container a {
            background: #7289DA;
            padding: 0 20px;
            line-height: 35px;
            color: #fff;
            font-family: 'Helvetica', 'Arial', sans-sefir;
            font-size: 20px;
            display: block;
            text-decoration: none;
            border-radius: 3px;
            top: 50px;
          }
          .container svg {
              width: 25px;
              height: 25px;
              color: #fff;
    opacity: 0.9;
    justify-self: center;
    justify-content: center;
    align-items: center;
    align-self: center;
    padding-top: 5px;
transform: translateY(4.5px) translateX(-3px);
          }
          .container svg:hover {
            animation: SvgAnimation 3s infinite;
            width: 30px;
    height: 30px;
          }
          @keyframes SvgAnimation {
0% {
    opacity: 0.9;
}
            25% {
    transform: translateX(5px);
}
50% {
    transform: translateY(5px);
}
75% {
    transform: translateX(-5px);
}
100% {
    transform: translateY(-5px);
}
}

body {
  margin: 0;
  font-family: Arial, Helvetica, sans-serif;
              background-color: #001f3f;
                color: #F5F5F5;
                    }
		.login-form {
			width: 300px;
			margin: 0 auto;
			font-family: Tahoma, Geneva, sans-serif;
		}
		.login-form h1 {
			text-align: center;
			color: #4d4d4d;
			font-size: 24px;
			padding: 20px 0 20px 0;
		}
		.login-form input[type="password"],
		.login-form input[type="text"] {
			width: 100%;
			padding: 15px;
			border: 1px solid #dddddd;
			margin-bottom: 15px;
			box-sizing:border-box;
		}
		.login-form input[type="submit"] {
			width: 100%;
			padding: 15px;
			background-color: #535b63;
			border: 0;
			box-sizing: border-box;
			cursor: pointer;
			font-weight: bold;
			color: #ffffff;
		}

.topnav {
  overflow: hidden;
  background-color: #333;
}

.topnav a {
  float: left;
  display: block;
  color: #f2f2f2;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
  font-size: 17px;
}
.info-section {
    border: 0 solid #f2f2f2;
    background-color: #111;
    color: #cacaca;
    padding: 5px 6px;
    border-radius: 5px;
    width: 100%;
    height: 100%;
    background-origin: border-box;
}
.info-section .right {
    text-align: right;
    align-items: right;
    align-content: right;
    align-content: right;
    flex-direction: right;
    background-position: right right;
    right: 0;
}
.topnav a:hover {
  background-color: #ddd;
  color: black;
}

.topnav a.active {
  background-color: #04AA6D;
  color: white;
}

.topnav .icon {
  display: none;
}

@media screen and (max-width: 600px) {
  .topnav a:not(:first-child) {display: none;}
  .topnav a.icon {
    float: right;
    display: block;
  }
}

@media screen and (max-width: 600px) {
  .topnav.responsive {position: relative;}
  .topnav.responsive .icon {
    position: absolute;
    right: 0;
    top: 0;
  }
  .topnav.responsive a {
    float: none;
    display: block;
    text-align: left;
  }
}
</style>
</head>
<body>

<div class="topnav" id="myTopnav">
  <a href="/dash" class="active">Dashboard</a>
  <a href="/command">Command</a>
  <a href="/guild">Guild</a>
  <a href="/shell">Shell</a>
  <a href="/djseval">DjsEval</a>
  <a href="/aoieval">AoiEval</a>
  <a href="javascript:void(0);" class="icon" onclick="myFunction()">
    <i class="fa fa-bars"></i>
  </a>
</div>
			<div class="container">
    <h1>HomePage</h1>
              </div>
              <% var last_side = "none" %>
              <% if(Array.isArray(info.body)) {%>
                <% for (var i = 0; i < info.body.length; i++) {%>
                    <% let SIDE = "right;"; %>
                    <% if(last_side === "right;" || i === 1 || i === 3) SIDE = "left;"  %>
                    <br>
                    <div id="section-<%=i%>" style="align-self: <%= SIDE %>text-align: <%= SIDE %>" class="info-section <%= SIDE === 'right;' ? 'right' : '' %>">
<h2> <%= info.body[i].title %></h2>
<br>
<p><%= info.body[i].description %></p>
<br>
<% console.log(SIDE, last_side)%>

<% if(SIDE === "right;") last_side = "left;"%>
<% if(SIDE === "left;") last_side = "right;"%>
<% console.log(SIDE, last_side)%>
</div>
                    <% } %>
                    <% } %>

<script>
function myFunction() {
  var x = document.getElementById("myTopnav");
  if (x.className === "topnav") {
    x.className += " responsive";
  } else {
    x.className = "topnav";
  }
}
</script>

</body>
</html>
